# ``Store_Primitives``

@Metadata {
    @DisplayName("Store Primitives")
    @TitleHeading("Swift Primitives")
}

The storage-column capability namespace: `Store.Protocol` (the seam every column type is written against), plus `Store.Ledgered` (the parity-token ledger discipline) and `Store.Initialization` (live-slot tracking).

## Topics
